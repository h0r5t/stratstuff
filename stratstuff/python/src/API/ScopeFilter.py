from src.engine.Scope import Scope


class ScopeFilter():

    # parses a scope and returns a new Scope with only the wanted (accepted) stuff
    # parse method is called everytime robot.getScope() is called

    def parse(self, scope):
        pass
